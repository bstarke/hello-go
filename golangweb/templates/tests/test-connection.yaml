apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "golangweb.fullname" . }}-test-connection"
  labels:
    {{- include "golangweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "golangweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
