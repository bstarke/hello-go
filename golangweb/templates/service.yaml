apiVersion: v1
kind: Service
metadata:
  name: {{ include "golangweb.fullname" . }}
  labels:
    {{- include "golangweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "golangweb.selectorLabels" . | nindent 4 }}
